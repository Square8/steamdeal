# Claude 작업 지침

작업 전에 `PROJECT_CONTEXT.md`와 `HANDOFF.md`를 읽는다.
관련 위치만 rg로 찾고, 생성 결과물과 DB를 불필요하게 읽지 않는다.
담당 파일만 수정하고 기존 미커밋 변경을 보존한다.
결과는 채팅뿐 아니라 HANDOFF.md에 지정된 보고서 파일에도 저장한다.
커밋·push는 명시적으로 요청받은 경우에만 한다.
