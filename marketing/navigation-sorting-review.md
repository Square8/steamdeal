# 홈 탐색 및 카테고리 정렬 — Claude R2 재검증

상태: **로컬 PASS(신규/기존 항목 전부) — Codex 최종 확인 전, 커밋 보류**. 검사일: 2026-09-17.
대상: `marketing/navigation-sorting-codex-review.md` "R2" 지시. Gemini R2 반영본(`build.py` 16:11 수정) 실제 코드로 검증. `selftest.py` exit 0(신규 실행, 결과 동일).

## 1) PASS — korean-soon 빌드 크래시 해결 확인
우회로 제거했던 `release_date` 픽스처(710401/402)를 복원해 전체 픽스처로 `build.main()`을 다시 돌렸고, 정상 종료 확인(예외 없음). 또한 경쟁 게임 없는 단독 DB(한국어+출시예정+`release_date` 게임 1개)로 별도 회귀 테스트를 추가해 직접 재현 시도 — **TypeError 재발 없음**. R1 재검증 때 남겼던 최우선 결함 해소됨.

## 2) PASS — "높은/낮은 가격순": 가격 미정 처리
`korean-games.html`(랜딩)과 홈 `#all` 양쪽에서 미정 2개(710706, 710902/710401·402 계열) + 명시적 무료(710705) + 실가격 다수를 넣고 낮은/높은 가격순을 실제 DOM으로 확인. **양쪽 방향 모두 미정 게임이 항상 맨 뒤**로 감(이전 재검증에서 발견한 "높은 가격순만 미정이 맨 앞" 재발 없음). 무료(0원)는 정상적으로 최저가로 정렬됨.

## 3) PASS — 홈 기본 상태 복귀(쿼리 제거 popstate)
`?sort=name#all`로 진입해 실제 이름순 정렬을 먼저 확인한 뒤, 쿼리 없는 URL로의 `popstate`를 직접 발생시켜 확인: select가 "추천순"으로 복귀할 뿐 아니라 **카드 실제 순서도 쿼리 없이 새로 연 페이지와 완전히 동일한 순서로 재정렬**됨(이전엔 select만 바뀌고 목록은 정렬된 채로 남는 문제였음). 무쿼리 첫 진입 시 `#all`이 뷰포트 밖이면 즉시 fetch하지 않는 지연 로딩도 그대로 유지됨을 별도 확인.

## 4) 기존 검증 항목 재확인(회귀 없음)
제목 가림(3뷰포트×7앵커), 직접 해시진입/새로고침/상세→홈, 메뉴 이동 시 UTM/q 보존, 무료 판정(textContent 아닌 data 필드, 이름에 "무료" 포함된 함정 게임 포함), 정렬 옵션 7종 라벨, 새로고침 후 정렬 유지, 잘못된 sort 값 폴백, 뒤로가기 후 정렬 상태 복원, Ctrl+클릭 새 탭, reduced-motion, 랜딩 설명문-실제 순서 일치, under-10000 경계값, 단독 데모앱 — **전부 PASS**.

## 로컬 검증 vs 라이브 배포
전부 임시 DB/SITE_DIR 기준 로컬 Playwright/직접 `build.main()` 검증. 라이브 배포 여부 미확인(이번 라운드 대상 아님).

## 변경 파일
- `tests/test_navigation_sorting.py`: 우회 제거하고 `release_date` 픽스처 복원, 단독 korean-soon 빌드 회귀 테스트·홈 기본 상태 복귀(popstate) 테스트 신규 추가.
- `marketing/navigation-sorting-review.md`(이 문서).
- 제품 코드·selftest.py·운영 DB 수정 없음. 커밋·push 없음.

## 남은 문제
없음(이번 R2 검증 범위 내). Codex 최종 diff 검토 및 커밋 대상/이름 안내 대기.
